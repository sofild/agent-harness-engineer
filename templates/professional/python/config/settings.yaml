# ============================================
# Agent 配置文件模板 - Professional
# ============================================
#
# ⚠ AI构建提示:
# 此为 schema 描述, 非完整 YAML 配置
# 根据需求生成以下节:
#   1. agent: 核心参数 (name, max_turns, model)
#   2. llm: 供应商配置 (provider, api_key_env, model, params)
#   3. tools: 工具白名单和参数
#   4. permissions: 权限规则列表
#   5. logging: 日志配置

# agent:
#   name: default
#   max_turns: 50

# llm:
#   provider: openai
#   api_key_env: LLM_API_KEY
#   model: gpt-4o
#   params:
#     max_tokens: 4096
#     temperature: 0.7

# tools:
#   enabled:
#     - read_file
#     - write_file
#     - grep
#     - glob
#     - bash

# permissions:
#   allow:
#     - path: "./workspace"
#       operations: [read, write]

# logging:
#   level: INFO
#   file: agent.log